<Page x:Class="alpha.CustomerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:alpha"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CustomerView">

    <!--#region DataContext etc -->
    <Page.DataContext>
        <local:CustomerViewModel/>
    </Page.DataContext>

    <!--#endregion-->
    <Grid Background="#f9f9f9">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="60"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0">
            <WrapPanel x:Name="FilterButtonList">
                <Button Style="{StaticResource FilterButton}" Command="{Binding FilterArticles}" CommandParameter="All" Content="Hem"/>
                <Button Style="{StaticResource FilterButton}" Command="{Binding FilterArticles}" CommandParameter="Pizza" Content="Pizza"/>
                <Button Style="{StaticResource FilterButton}" Command="{Binding FilterArticles}" CommandParameter="Sallad" Content="Sallad"/>
                <Button Style="{StaticResource FilterButton}" Command="{Binding FilterArticles}" CommandParameter="Dryck" Content="Dryck"/>
            </WrapPanel>
            <!--
            
            Todo; Makes this below work?
            
            
            <Button x:Name="FilterButton" Command="{Binding FilterArticles}" Content="TEST__TRIGGER" />
            <Border BorderBrush="#FF196861" BorderThickness="2,2,2,2">
                <ListView x:Name="FilterButtonList" 
                          ItemsSource="{Binding Buttons, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectionMode="Single"
                          >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="FilterButton"  Command="{Binding FilterArticles}" Content="{Binding }" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>-->
        </StackPanel>


        <StackPanel Grid.Row="1" Grid.Column="0">


            <ListView 
                  ItemsSource="{Binding Articles, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedArticle, Mode=TwoWay}"
                  >

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>


                            <TextBlock Grid.Row="1" Grid.Column="1"  Text="{Binding Article.Name}"/>
                            <TextBlock Grid.Row="1" Grid.Column="2"  Text="{Binding Article.Price}"/>

                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <!--<ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"  DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Price"  DisplayMemberBinding="{Binding Price}"/>
                </GridView>
            </ListView.View>-->
            </ListView>
            <Button Command="{Binding ChangeArticle}" Width="80" Height="40"  Content="Uppdatera Collection"/>
            <TextBlock Text="{Binding SelectedArticle.Article.Name}" Background="#FFE4E4E4" MaxWidth="200"/>
        </StackPanel>


        <StackPanel Grid.Column="1" Grid.RowSpan="2" Background="#afafaf">
            
        </StackPanel>
        
    </Grid>
</Page>
